using System.Collections.Generic;

namespace WebApp1.Models
{
    public enum Cyklus
    {
        A = 0,
        B = 1,
        C = 2,
    }

    public enum TypId
    {
        Doba = 1,
        Svatek = 2,
        LitText = 3,
    }

    public enum AllNothing
    {
        None = -1,
        All = 0
    }
}
